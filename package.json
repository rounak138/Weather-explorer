{
  "name": "weather-explorer",
  "version": "1.0.0",
  "description": "Full-stack Weather Explorer Dashboard",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install --include=dev",
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && npm --prefix backend install",
    "start": "node backend/src/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
